spring:
  application:
    name: configclient
  profiles:
    # 拼接目标配置文件名
    active: dev
  cloud:
    config:
      # 本地 Config Server 的访问路径
      uri: http://localhost:8063
      # 设置客户端优先判断 Config Server 获取是否正常。
      fail-fast: true