server:
  # 当前服务端口
  port: 8050
spring:
  application:
    name: feign
eureka:
  client:
    # 注册中心的访问地址
    service-url:
      defaultZone: http://localhost:8062/eureka/
  instance:
    # 是否将当前服务的ip注册到 Eureka Server
    prefer-ip-address: true

# 是否开启熔断器。
feign:
    hystrix:
      enabled: true