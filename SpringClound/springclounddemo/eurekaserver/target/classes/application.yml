server:
  # 当前服务端口
  port: 8062
spring:
  application:
    name: eureka-server
eureka:
  client:
    # 是否要将当前服务作为客户端进行注册
    register-with-eureka: false
    # 是否要获取同步其他的Eureka server 服务的数据
    fetch-registry: false
    # 注册中心的访问地址
    service-url:
      defaultZone: http://localhost:8062/eureka/

